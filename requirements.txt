Flask==3.1.3
Werkzeug==3.1.7
# Production WSGI server. Only installs on Linux/Mac (your deployment host) --
# gunicorn doesn't support Windows, so this is skipped automatically on a
# Windows "pip install" and never affects local development there.
gunicorn==23.0.0; sys_platform != "win32"
